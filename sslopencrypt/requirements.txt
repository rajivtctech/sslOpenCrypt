# sslOpenCrypt — Python dependencies
# Install with: pip install -r requirements.txt

# GUI framework (GPL v3 compatible)
PyQt6>=6.4.0

# Cryptographic parsing and validation (used for display, NOT for operations)
cryptography>=41.0.0

# Memory-hard key derivation (key vault, passphrase protection)
argon2-cffi>=21.3.0

# PDF signing support (Module 5 - PAdES, planned v1.0)
# endesive>=2.10.0

# Testing
pytest>=7.0.0
pytest-qt>=4.0.0
